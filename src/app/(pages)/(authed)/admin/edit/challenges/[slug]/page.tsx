export default function AdminChallengePage ({ params }: { params: { slug: string } }) {

    let challenge = null;

    return (
        <div>
                admin page for challenge.
        </div>
    )
}